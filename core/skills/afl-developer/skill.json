{
  "slug": "afl-developer",
  "name": "AmiBroker AFL Developer",
  "description": "DISABLED — AFL generation is handled exclusively by ClaudeAFLEngine via POST /afl/generate or the chat tool generate_afl_code. This skill record is kept on disk only for reference.",
  "category": "code",
  "tools": ["execute_python"],
  "output_type": "code",
  "max_tokens": 32000,
  "timeout": 18000,
  "enabled": false,
  "aliases": []
}

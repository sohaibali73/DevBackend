# =============================================================================
# requirements.txt — BULLETPROOF EDITION
# For FastAPI Python backend hosted on Railway
# =============================================================================

# Web Framework
fastapi
uvicorn[standard]
gunicorn

# AI
anthropic>=0.84.0
openai>=1.0.0
httpx                   # http2 optional via core/http_client.py (auto-fallback)
h2                      # enables httpx HTTP/2 multiplexing for LLM calls



# Database
supabase
asyncpg>=0.29.0          # direct async Postgres driver for hot-path queries

# Cache
redis>=5.0.0             # async Redis client (uses redis.asyncio)

# JWT (local decode for fast auth — avoids round-trip to Supabase on every request)
PyJWT[crypto]>=2.8.0

# Research
tavily-python
yfinance
requests

# Auth & Encryption
python-jose[cryptography]
passlib[bcrypt]
cryptography>=41.0

# =============================================================================
# DOCUMENT PROCESSING
# =============================================================================

# Core parsers
pypdf
python-docx
PyMuPDF
python-pptx>=0.6.21
Pillow>=10.0.0

# PPTX Intelligence Pipeline (computer vision + perceptual hashing)
imagehash>=4.3.1          # perceptual hash for design element library matching

# Bulletproof universal parser (magic bytes + 64+ formats + OCR + archives)
filetype
unstructured[all-docs]

# Fallbacks
pdfplumber
openpyxl
pytesseract

# =============================================================================
# UTILS
# =============================================================================

python-dotenv
pydantic
pydantic-settings
python-multipart
aiofiles
email-validator
PyYAML>=6.0          # SKILL.md frontmatter parsing for user-uploaded skills

# =============================================================================
# DATA ANALYSIS
# =============================================================================

numpy
pandas

# =============================================================================
# AUDIO / VOICE
# =============================================================================

edge-tts
SpeechRecognition
pydub

# =============================================================================
# EMBEDDINGS (vector search)
# =============================================================================

sentence-transformers==2.2.2

# =============================================================================
# SANDBOX PERSISTENCE & UTILITIES
# =============================================================================

aiosqlite                        # async SQLite driver for sandbox DB
faker                            # fake data generation (Faker class in sandbox)

# =============================================================================
# LLM SANDBOX (isolated code execution)
# =============================================================================

llm-sandbox
docker

# =============================================================================
# CONTENT STUDIO — humanizer detector ensemble (heavy)
# =============================================================================
# Optional heavy stack for the AI-detection ensemble (Binoculars + GLTR + Roberta).
# Models lazy-load to $STORAGE_ROOT/models/hf_cache and are cached on the Railway volume.
# If torch+transformers are unavailable, the humanizer falls back to the lightweight
# stylometric detector (no functional break — just less accurate).

torch>=2.0,<3.0
transformers>=4.40,<5.0

